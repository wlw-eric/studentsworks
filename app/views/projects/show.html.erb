
<br>
<div class="text-center">
  <%= link_to ' Edit', edit_project_path(@project), class: "far fa-edit btn btn-primary btn-circle btn-lg" %>
  <%= link_to ' Participe', edit_project_path(@project), class: "  far fa-plus-square btn btn-primary btn-circle btn-lg" %>
 </div>
<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-md-6" >
    <h1>
      <strong><%= @project.name %></strong>
    </h1>
    <h5>
      <strong>Category:</strong>
      <%= @project.category %>
    </h5>
    <h5>
      <strong>Objective:</strong>
      <%= @project.objective %>
    </h5>
  </div>


    <div class="col-md-3">
      <img src=<%= @project.picture %> class="card-img-top" alt="projet">
    </div>
</div>

    <br>
    <p>
      <strong>Description:</strong>
      <%= @project.description %>
    </p>
<!-- <p>
  <strong>Progress:</strong>
  <%= @project.progress %>
</p> -->
<% logo = `<span class="far fa-edit"></span>` %>

